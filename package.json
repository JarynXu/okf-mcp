{
  "name": "@open-knowledge-stack/okf-mcp",
  "version": "0.2.0-alpha.1",
  "description": "MCP stdio server for Open Knowledge Format bundles and pluggable Libraries",
  "license": "Apache-2.0",
  "type": "module",
  "bin": { "okf-mcp": "src/server.mjs" },
  "files": ["src", "README.md", "LICENSE", "NOTICE"],
  "engines": { "node": ">=20" },
  "scripts": {
    "lint": "node --check src/server.mjs && node --check src/protocol.mjs && node --check src/tools.mjs && node --check src/cli.mjs",
    "test": "node --test test/cli.test.mjs test/protocol.test.mjs test/stdio.test.mjs",
    "check": "npm run lint && npm test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/open-knowledge-stack/okf-mcp.git"
  }
}
